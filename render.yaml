services:
  - type: web
    name: telegram-bot
    runtime: python
    buildCommand: ""
    startCommand: python bot.py
    envVars:
      - key: BOT_TOKEN
        value: your_token_here
    plan: free
